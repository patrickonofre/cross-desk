# STATE — CrossDesk macOS

## Decisões

- **2026-07-03 — macOS 14+ como alvo mínimo** (habilita MenuBarExtra estável, SMAppService, APIs modernas de Network.framework). Assumido, não confirmado com usuário — fácil de baixar para 13 se preciso.
- **2026-07-03 — App única com dois papéis** (servidor OU cliente, toggle na UI), não duas apps.
- **2026-07-03 — Lógica em SPM packages locais** (CrossDeskKit), app Xcode só como casca — testes rodam via `swift test` sem GUI.

## Bloqueios

- Nenhum.

## Decisões (cont.)

- **2026-07-03 — DTLS-PSK confirmado (T1 ✅)**: Network.framework faz DTLS 1.2 + `TLS_PSK_WITH_AES_128_GCM_SHA256` nativamente. Auth por código de pareamento gerado (128 bits) → HKDF → PSK. Certificados/TOFU descartados. QUIC fallback não necessário.

- **2026-07-03 — App como executável SPM + make-app.sh** (SPEC_DEVIATION do design "casca Xcode"): bundle montado por script, assinatura ad-hoc. Registrado em tasks.md T14.

- **2026-07-03 — Geometria independente por máquina (T16)**: borda de retorno detectada no CLIENTE (`LEAVE_REQUEST` 0x12), `VirtualCursor` do servidor removido. Motivo: simulação server-side driftava com resoluções diferentes (cursor "se perdia" no UAT). Cliente agora contém o cursor em TODOS os seus monitores (`ScreenTopology`). Resolução continua fora do fio (R8 preservado).

- **2026-07-03 — Ocultação de cursor via API privada aprovada (input-polish R17)**: `SetsCursorInBackground` (`CGSSetConnectionProperty` via `dlsym`) + `CGDisplayHideCursor` — mesma técnica de Deskflow/Barrier. Provado no spike T19 (macOS 26: símbolos resolvem, CGError=0). Fallback: seta parada-visível (warp-offscreen NÃO serve — clampa). Símbolos SEMPRE via `dlsym`, nunca linkados. App já é não-sandbox (tap com supressão), então API privada não muda o status de distribuição.
- **2026-07-03 — Trava de cursor não confia só na dissociação (input-polish R18/R19)**: `CGAssociateMouseAndMouseCursorPosition(0)` é estado global volátil (cai em sleep/wake). Defesa em camadas: servidor re-warpa a cada move capturado; cliente tem watchdog (`CursorSentinel`, poll 250 ms, sem TCC novo); ambos re-aplicam em wake/screensaver/display via `SystemStateObserver`. Padrão confirmado nos dois pares maduros (Deskflow/lan-mouse re-warpam continuamente).

## Decisões (cont. 2)

- **2026-07-05 — layout-ux especificada e aprovada pelo usuário**: UX espacial no lugar do formulário — mini-mapa vivo no painel, janela "Telas" com monitores locais reais (`Displays` + `CGDisplayIsBuiltin`) e peer como tile abstrato arrastável (4 lados → `edgeSide`), estados vivos (foco/travessia/transferência), ícone dinâmico. Pesquisa: UC (travessia = onboarding), Synergy 3 (canvas livre), grade Synergy1/Deskflow = anti-padrão (#4173). Zero mudança de protocolo (§5 vira feature: peer abstrato). R35–R42 em `.specs/features/layout-ux/`. Mockup interativo apresentado na sessão. Ideia adiada: canvas bilateral em escala (exigiria geometria no fio).

- **2026-07-04 — discovery-pairing especificada** (viabilidade confirmada): descoberta Bonjour nativa (`NWListener.Service` + `NWBrowser`, `_crossdesk._udp`, zero deps) + pareamento por token curto (8 chars, ~40 bits) com rotação para segredo 128-bit via PAIR_SET/PAIR_ACK (0x05/0x06) dentro do túnel DTLS. Decisões do usuário: token curto+rotação, reconexão automática, fallback manual por IP mantido. Risco aceito: brute-force offline do token na janela de pareamento (documentar no PROTOCOL.md; PAKE elimina na Fase 4). Docs: `.specs/features/discovery-pairing/` (spec/context/design/tasks T29–T38).

## Decisões (cont. 3)

- **2026-07-05 — code review profundo de todo o CrossDeskKit/App** (168 arquivos-fonte, ~6.4k linhas — cobertura completa, não amostragem): 8 bugs reais corrigidos, todos com teste ou build de verificação; suite subiu de 166 → 168 testes verdes; release build (`make-app.sh`) limpo, zero warnings. Nenhuma lacuna de escopo pendente identificada nesta passada — próxima revisão profunda só faz sentido após a próxima leva de features (Fase 2/3) ou se surgir um bug em campo. Achados:
  - `DTLSClient.remoteHost()` lia `connection` fora da `queue` de confinamento (data race) — agora `queue.sync`.
  - `ServerBrowser.restart()` podia reviver o browser depois de um `stop()` (retry de 1s agendado antes do stop, sem guarda) — contador de geração incrementado no `stop()`.
  - `TransferCoordinator.updateFilePSK` reconstruía o listener sem retry — mesma classe de falha (bind race, EADDRINUSE) já resolvida no `DTLSServer`, mas o file-channel ficava mudo até reiniciar a app. Retry com backoff + guarda `stopped`/rotação superada.
  - `TransferCoordinator.receiveNow()` não checava `active == nil` — clicar "Receber agora" durante outra transferência descartava a oferta pendente silenciosamente.
  - `AppState.connect(to:)` gravava `config.pairedServerName` ANTES do PAIR_SET confirmar — se o usuário digitasse o token errado ou a troca falhasse, o secret antigo ficava associado ao nome errado. Agora `pendingServerName` só vira `pairedServerName` no callback `onPairSet`.
  - `Message.hello` e `FileChannelMessage.error`: truncagem de nome/mensagem por contagem de bytes (`utf8.prefix(N)`) podia cortar um caractere multi-byte ao meio (acento comum em nome de Mac). O lado receptor falha `String(data:encoding:.utf8)` e o `decodeAll` derruba o datagrama INTEIRO, não só o campo — silencioso. Fix: `WireStrings.utf8Prefix` (Sources/Protocol/Message.swift) recua byte a byte até achar um limite de escalar válido. Testes de regressão com "á" repetido no boundary exato (`MessageTests.testHelloNameTruncationNeverSplitsAMultiByteCharacter`, `FileChannelMessageTests.testErrorMessageTruncationNeverSplitsAMultiByteCharacter`).
  - `NetworkInfo.localIPv4Addresses()`: `String(cString:)` sobre `[CChar]` é a via deprecated (warning no build release) — trocado para a variante de ponteiro (`withUnsafeBufferPointer`).
  - `FileReceiver.handle`: `var item` sem mutação (warning) → `let`.
- **2026-07-05 — beta.10 publicada (build 11), tag `v0.1.0-beta.10`**: bundle inclui, além dos fixes do code review acima, as duas features code-complete que nunca tinham ganhado build/tag próprios — file-transfer E1 (T39–T46) e layout-ux (T49–T56), ambas herdadas da beta.9 (build 10). É o "build novo" do passo 0 do roteiro [UAT-2026-07-06.md](UAT-2026-07-06.md) — UATs T38/T47/T57/T28 continuam pendentes (sessão de 2 macs), rodar contra este build.

- **2026-07-06 — investigado "app fechou sozinho ao conectar" (UAT)**: `log show` no incidente não achou `.ips` nem sinal — sequência de `terminate:` limpa (`applicationShouldTerminate:` → `NSTerminateNow`), ou seja, não foi crash. Só achado colateral real: fault do SwiftUI `No symbol named 'display.2.fill' found in system symbol set` (`AppState.menuBarSymbol`) — corrigido (agora usa `.symbolVariant(.fill)` em vez de concatenar o nome, nunca falha mesmo se a variante não existir no symbol set do sistema). Causa exata do fechamento não fechada com certeza — só há dois call-sites de `terminate(nil)` (botão Sair, `relaunch()`) e nenhum logava antes; ambos ganharam log agora, mais um `NSApplicationDelegate.applicationWillTerminate` genérico (`AppTerminationLogger` em `CrossDeskApp.swift`) que pega qualquer caminho de saída (⌘Q incluso) — próxima ocorrência fecha com certeza. Motivou a feature debug-console abaixo.
- **2026-07-06 — debug-console especificada e implementada**: atalho global oculto `Cmd+Shift+D` (`RegisterEventHotKey` — única API de hotkey global sem exigir Accessibility, confirmado via busca; zero-dep, no estilo `PairingKey.swift`) abre um console flutuante com o `log stream` do subsystem `dev.crossdesk.mac` ao vivo (reaproveita o comando já documentado em `Logging.swift`, via `Process`/`Pipe` — mesmo padrão do `AppState.relaunch()`). Sem entrada em menu nenhum, presente em todo build incluindo release (`make-app.sh`) — é o que roda em UAT real. R43–R49 em `.specs/features/debug-console/spec.md`. Novos arquivos: `Sources/Debug/{DebugLogBuffer,LogStreamer,DebugHotKey}.swift` + `App/DebugConsoleWindow.swift`, testes reais (não-mock) em `Tests/DebugTests/` (`LogStreamerTests` chega a rodar um `log stream` de verdade e captura uma linha marcada em <2s). 173 testes verdes (168→173). Verificado ponta-a-ponta com keypress global de verdade (via computer-use, ⌘+Shift+D com o Finder em foco, não o CrossDesk) — o compositor confirmou uma janela nova pertencente a `dev.crossdesk.mac`, e o subprocesso `log stream` nasceu como filho direto do PID do app; fechar de novo matou o subprocesso sem zumbi.
- **2026-07-06 — causa raiz real do "app fechou sozinho" encontrada durante o teste do debug-console**: reproduzida ao vivo — app parado, sem janela visível (papel padrão de app menubar-only), foi morto pelo próprio AppKit (`com.apple.AppKit:AutomaticTermination`, `_kLSApplicationWouldBeTerminatedByTALKey`) depois de ~65s parado em segundo plano, com a sequência exata de `terminate:`/`applicationShouldTerminate:`/`NSTerminateNow` já vista no incidente do UAT — sem Sair, sem ⌘Q (confirmado pelo `AppTerminationLogger` novo: `applicationWillTerminate` disparou sem o log do botão Sair antes). Causa: apps `LSUIElement` sem janela visível são candidato natural do mecanismo de "Automatic Termination" do AppKit (mata processo em segundo plano ocioso). Fix: `ProcessInfo.processInfo.disableAutomaticTermination(...)` no `init()` do `CrossDeskApp` — opt-out permanente, uma linha. Corrigido e reverificado (app sobreviveu ao mesmo período de espera após o fix). **Fecha, com alta confiança, a investigação aberta no início da sessão de 2026-07-06** sobre o fechamento durante o UAT.
- **2026-07-06 — beta.11 publicada (build 12), tag `v0.1.0-beta.11`**: bundle = os três achados acima (fault do ícone da menubar, instrumentação de saída do app, causa raiz + fix do fechamento automático) + a feature debug-console (R43–R49) nova. Nenhuma mudança de protocolo — só client macOS. 173 testes verdes.
- **2026-07-06 — token curto de pareamento (R28) reduzido de 8 para 6 chars**: `PairingKey.generateShortToken()` agora gera `XXX-XXX` (3+3) em vez de `XXXX-XXXX` (4+4), ≈29 bits em vez de ≈39. Atualizado em código (`PairingKey.swift`, `PairingKeyTests.swift` — contagem/format), UI (`MenuBarView.swift` placeholder do campo token) e specs (`discovery-pairing/spec.md` R28 + análise de segurança, `discovery-pairing/design.md`, `PROTOCOL.md` §1 + changelog na raiz). `normalize()` já era agnóstico a tamanho — nenhuma outra lógica dependia do comprimento de 8. Token não trafega no fio (só deriva PSK local), então é mudança sem impacto de compatibilidade entre implementações.
- **2026-07-06 — bug real achado logo depois (usuário reportou "não consegue conectar")**: `config.json` desta máquina tinha `pairingCode` = código antigo de 32 hex chars (fluxo manual pré-discovery-pairing) — servidor exibia esse blob inteiro em vez de um token curto, porque o guard de regeneração em `AppState.init()` e no `onChange` do Picker de papel (`MenuBarView.swift`) só disparava com `pairingCode.isEmpty`; um valor antigo não-vazio sobrevive para sempre. Fix: `PairingKey.isShortToken(_:)` (novo) valida formato/alfabeto/tamanho real (6 chars do `tokenAlphabet`) em vez de só checar vazio — os dois call-sites trocaram `.isEmpty` por `!isShortToken(...)`. Autocura confirmada ao vivo: relançar a app reescreveu o `config.json` com um token válido (`ZSX-8PP`) no lugar do hex antigo. 175 testes verdes (+2: `testIsShortTokenAcceptsGeneratedTokensInAnyCaseOrSpacing`, `testIsShortTokenRejectsStaleLongFormCode`). Lição: qualquer campo persistido com "gerar só se vazio" precisa validar a FORMA esperada, não só a ausência — reaparece em qualquer config antigo de antes de uma feature existir ou de um formato mudar.
- **2026-07-06 — ícone do app (placeholder)**: bundle não tinha `CFBundleIconFile` nem asset nenhum (ícone genérico de executável no Finder). Usuário escolheu gerar via SF Symbol (sem asset de marca no repo ainda; nome do produto continua "working title"). Novo `Scripts/make-icon.swift` (roda com `swift`, sem Xcode/asset catalog) reaproveita `display.2` — o mesmo glifo do ícone da menubar (`AppState.menuBarSymbol`) — tingido de branco sobre fundo squircle azul (gradiente), rasterizado nos 10 tamanhos padrão + `iconutil` → `.icns`. Achado no caminho: tingir via `sourceAtop` só funciona ANTES de compor sobre um fundo opaco — depois que o retângulo do glifo já está sobre o fundo, todo pixel ali (bezel, tela e os vãos transparentes) lê alpha=1 e o tint branqueia o retângulo inteiro (virou bug visual real, pego só porque cada estágio foi conferido visualmente com o Read tool antes de aceitar). Fix: tingir num canvas transparente isolado primeiro, só depois compor a imagem já branca sobre o fundo. Saída commitada em `macos/Resources/AppIcon.icns` (fonte versionada, já que `macos/build/` é gitignored); `make-app.sh` copia pra `Contents/Resources/` e seta `CFBundleIconFile`. Trocar quando a identidade visual real for definida (mesma pendência do nome do produto).
- **2026-07-06 — beta.12 publicada (build 13), tag `v0.1.0-beta.12`**: bundle = token curto de pareamento reduzido pra 6 chars (R28) + fix do bug de migração (`pairingCode` antigo de 32 hex nunca virava token curto) + ícone do app (placeholder SF Symbol). Nenhuma mudança de protocolo que quebre compatibilidade (`proto_version` continua 1) — interopera com beta.11. 175 testes verdes. É o build que corrige o bloqueio real encontrado em cima da hora pro UAT consolidado de hoje ([UAT-2026-07-06.md](UAT-2026-07-06.md), bloco A/T38) — rodar contra este build.
- **2026-07-06 — bug real achado AO VIVO durante o UAT com a beta.12**: cliente fechava sozinho ao clicar no campo de token de pareamento. `log stream` capturado em tempo real (mesma técnica do debug-console) mostrou `-[NSApplication terminate:]` limpo — sem crash, sem sinal — mas nenhum dos 2 únicos call-sites de `terminate()` no código (botão "Sair", `relaunch()`) logou antes, o que os descarta como causa. Causa real: SwiftUI registra um comando "Quit" (⌘Q) por padrão pra qualquer `App`, mesmo `LSUIElement` sem menu bar visível — ele chama `terminate()` direto, nunca passa pelo botão "Sair", por isso nunca loga. Fix: `CommandGroup(replacing: .appTermination) {}` no `CrossDeskApp` remove esse Quit implícito — "Sair" (que loga) passa a ser o único jeito de fechar o app. 175 testes verdes. Achado colateral no caminho: a build rodando durante o UAT estava com **App Translocation** ativo (`LSBundlePath` sob `.../AppTranslocation/.../CrossDesk.app`) — quarentena do zip baixado não foi removida com `xattr -cr` antes de abrir; reforçar esse passo no roteiro de instalação.
- **2026-07-06 — beta.13 publicada (build 14), tag `v0.1.0-beta.13`**: bundle = só o fix do Quit implícito acima. Nenhuma mudança de protocolo. 175 testes verdes. Build de emergência em cima do UAT ao vivo — rodar contra este build (não a beta.12).
- **2026-07-06 — robustez contra VPN**: causa raiz da instabilidade da beta.13 confirmada com o usuário — VPN corporativa numa das máquinas. Duas mudanças: (1) `DTLSParameters.make`/`FileChannelParameters.make` ganharam `parameters.prohibitedInterfaceTypes = [.other]` — `.other` é a única classificação que Network.framework dá a interfaces virtuais/desconhecidas (`utun` de VPN incluso; confirmado via busca + doc da Apple, sem case dedicado de VPN), então o transporte agora recusa rotear por qualquer túnel, sempre LAN física (Wi-Fi/Ethernet). (2) `VPNMonitor` novo (`NWPathMonitor` + `path.usesInterfaceType(.other)`) alimenta `AppState.vpnActive`, com aviso não-bloqueante na UI (`MenuBarView.vpnWarning`). Verificado ao vivo nesta máquina: `usesInterfaceType(.other)` retorna `false` mesmo com `utun0–3` do sistema presentes (Handoff/Hotspot, dormentes) — só acende com VPN de verdade roteando, sem falso positivo. 177 testes verdes (+2: `InterfaceRestrictionTests`). Loopback (`TransportLoopbackTests`) continua passando — `.loopback` é um tipo distinto de `.other`, não afetado.
- **2026-07-06 — beta.14 publicada (build 15), tag `v0.1.0-beta.14`**: bundle = só a robustez contra VPN acima (transporte recusa rotear por túnel + aviso na UI). Nenhuma mudança de protocolo. 177 testes verdes. Rodar contra este build na máquina que tem VPN de trabalho.
- **2026-07-07 — revisão do ciclo de vida do app (usuário pediu explicitamente)**: compartilhamento funcionou de ponta a ponta na beta.14 (VPN fix confirmado), mas clicar no ÍCONE da menubar (fechar o popover) fechou o app inteiro. Log ao vivo (mesmo `log stream` capturando desde ontem) mostrou `applicationWillTerminate` de novo sem nenhum dos 2 call-sites conhecidos (nem "Sair", nem `relaunch()`) — ou seja, o `CommandGroup(replacing: .appTermination)` da beta.13 não cobre esse caminho, porque não é o Quit do menu. Pesquisa (web) achou o bug real: **Apple FB11447959** — combinar `Window` + `MenuBarExtra` no SwiftUI tem bug de contagem de scene documentado que pode levar o AppKit a decidir "nenhuma cena restante, terminar" e chamar `terminate()` direto, sem passar pelo comando de Quit. Bate exato com nossa arquitetura (`Window("Telas")` + `MenuBarExtra` no `CrossDeskApp`). Fix mais robusto que o anterior: `AppTerminationLogger` (o `NSApplicationDelegate` já existente) ganhou `applicationShouldTerminate(_:)` que só permite terminar se `deliberateQuitRequested` foi setado explicitamente pelos 2 call-sites deliberados (Sair, `relaunch()`) — qualquer outro caminho (esse bug do SwiftUI, um futuro Cmd+Q se a remoção do comando falhar por algum motivo, qualquer coisa que ninguém previu) é CANCELADO e logado como bloqueado. Isso é o gate real — cobre a causa raiz em vez de tapar buracos um por um. 177 testes verdes (sem teste automatizado pro bug do SwiftUI em si — não dá pra simular via XCTest; verificação é ao vivo, próxima sessão de uso confirma).
- **2026-07-07 — beta.15 publicada (build 16), tag `v0.1.0-beta.15`**: bundle = só o gate de `applicationShouldTerminate` acima. Nenhuma mudança de protocolo. 177 testes verdes. Rodar e testar especificamente abrir/fechar o popover pelo ícone da menubar várias vezes seguidas.
- **2026-07-07 — autostart especificada** (item já previsto em `PROJECT.md` como "pós-MVP"): toggle "Abrir ao iniciar sessão" via `SMAppService.mainApp` (register/unregister), sem campo novo em `AppConfig` — a UI lê/escreve o status real do SO direto, mesma lição do TCC (não duplicar fonte de verdade). `.requiresApproval` tratado com o mesmo padrão de aviso+botão já usado pra Input Monitoring/Accessibility. Design dispensado (decisão de API já tomada). R50–R54 + T58–T61 em `.specs/features/autostart/`.
- **2026-07-07 — autostart CODE-COMPLETE (T58–T60 ✅), falta UAT (T61)**: `LoginItem.swift` (wrapper fino sobre `SMAppService.mainApp`, zero estado próprio) + `AppState.loginItemEnabled`/`loginItemNeedsApproval`/`refreshLoginItemStatus()`/`toggleLoginItem()` + toggle e aviso de aprovação em `MenuBarView.optionsSection`. 177 testes verdes (sem teste novo — API do SO, mesmo caso de `Permissions.swift`), `make-app.sh` limpo sem warnings novos. Smoke test manual parcial: app buildado rodou e saiu limpo; **não** deu pra clicar o toggle via computer-use (o resolver de apps do MCP não reconhece o build dev — não está instalado/indexado pelo Launch Services) — toggle real, aprovação em Ajustes > Itens de Login e o restart (aceitações 1–6 do spec) continuam pendentes de UAT com o usuário.
- **2026-07-07 — os 5 UATs pendentes fechados**: usuário rodou o roteiro `UAT-1.0.md` (2 macs) e reportou os 5 blocos passando sem bugs — discovery-pairing (T38, aceitações 1–8), file-transfer E1 (T47, aceitações 1,2,4,7,8), input-polish (T28, aceitações + regressão), layout-ux (T57, R35–R41), autostart (T61, aceitações 1–6 incl. restart real). Registrado no nível de bloco (não item-a-item — usuário não detalhou cada checklist individual; sem achado de bug relatado, então não há entrada específica de fix aqui). Nota: o veto antigo sobre o símbolo `cursorarrow.motionlines` (REMOTE, registrado no UAT anterior de layout-ux) não foi mencionado como problema neste passe — assumindo aceito/não-bloqueante. Fecha o gate de qualidade do `release-1.0` (Fase 1) — segue para Fases 2–5 (versão, tags, publicação).

- **2026-07-07 — investigado "Cmd+Shift+D não fez nada" reportado pelo usuário controlando o cliente remotamente via KVM**: revisão do relay de teclado (`HIDKeycodes`/`InputCapture.modifierMask`/`InputInjector.updateFlags`) confirma Control e Option mapeados corretamente nos dois sentidos (mesma função reusada client/server). Fn (`0x3F`) segue **intencionalmente não mapeado** (doc + teste `HIDKeycodesTests` já cobrem isso — não é bug). Para Cmd+Shift+D especificamente: é o hotkey do debug-console (R43), local a cada máquina (`RegisterEventHotKey`, não passa pelo protocolo/relay) — rastreado o motivo mais provável de falha silenciosa: `DebugHotKey.register()` descartava o `OSStatus` de `RegisterEventHotKey`; se outro app no cliente já registrou essa combinação (Rectangle/Alfred/Karabiner/BTT são comuns), o registro falha e o atalho nunca dispara, sem nenhum log — irônico pra a ferramenta de debug. Fix: loga a falha (`Log.app.error`) quando `status != noErr`. 190 testes verdes, sem teste novo (API do SO, mesmo padrão de `Permissions.swift`). **Não verificado nesta sessão**: se o keypress SINTETIZADO via `CGEventPost` (chegando pelo relay, não do teclado físico do cliente) de fato dispara o Carbon Hot Key Manager — a única verificação ponta-a-ponta registrada até agora (2026-07-06) foi com keypress físico real. Requer teste ao vivo com 2 máquinas: se Cmd+Shift+D funcionar direto no teclado do cliente mas não via relay do servidor, é essa a lacuna.

- **2026-07-07 — hotkey do debug-console trocado de Cmd+Shift+D para Cmd+Shift+\\** (decisão do usuário: colidia com atalhos de ferramentas de desenvolvimento). Mudança mecânica: `DebugHotKey.register(keyCode:)` default (`kVK_ANSI_D` → `kVK_ANSI_Backslash`), comentários em `CrossDeskApp.swift`/`DebugConsoleWindow.swift`, R43/R46/"Decisões assumidas" em `debug-console/spec.md`. Achado no caminho: string Swift com `\` cru quebra escape — precisa `\\` no literal (só apareceu na string de log da sessão anterior; comentários `///` não são afetados). 190 testes verdes, sem teste novo (mesmo padrão anterior — API do SO).

## Pendências

- [ ] Verificar ao vivo (2 macs) se Cmd+Shift+\\ sintetizado via `CGEventPost` (chegando pelo relay) dispara o `RegisterEventHotKey` do cliente — testar local vs. remoto separadamente pra isolar. Ver decisão 2026-07-07 acima (investigação original, atalho era D na época).
- [x] ~~autostart/layout-ux/file-transfer E1/discovery-pairing/input-polish — UATs pendentes~~ — **FECHADOS 2026-07-07**, ver decisão abaixo.
- [x] Golden vectors → `.specs/protocol/vectors/v0_1.txt` (provados por teste).
- [ ] Spike T2 (CGEventPost fora da main thread + Esc em REMOTE) — valida na prática durante T15/UAT.
- [ ] Pairing code em JSON plano → migrar para Keychain pós-MVP.
- [x] ~~Assinatura ad-hoc: TCC re-pedindo após rebuild~~ — RESOLVIDO (beta.3): identidade self-signed "CrossDesk Dev" no keychain da máquina de build, make-app.sh auto-detecta (fallback ad-hoc). Cert Apple Developer real continua desejável p/ notarização (elimina o passo do xattr).
  - ~~Cursor visível parado na borda~~ → R17 (`CursorConcealer`, API privada `SetsCursorInBackground` provada no spike T19).
  - ~~Seta anda após standby/wake~~ → R18 (`CursorSentinel` watchdog) + R19 (`SystemStateObserver` reassert).

## Lições (cont.)

- Simular a posição do cursor remoto no servidor (duas contabilidades da mesma verdade) drifta assim que as resoluções diferem — quem injeta o cursor é a única fonte de verdade da posição dele.
- Adicionar campo não-opcional a um `Codable` persistido (config.json) quebra o load de configs antigos (falta a chave → decode throws) — `ConfigStore` trata throw como "corrompido". Cura: `init(from:)` com `decodeIfPresent ?? default` (tolerante a chaves ausentes, mantém throw só p/ JSON inválido). Feito no `concealCursor`.
- `NSEvent.addGlobalMonitorForEvents` exige **Accessibility**, não Input Monitoring — por isso o watchdog do cliente (que só tem Accessibility p/ injetar) usa poll de `CGEvent(source: nil)?.location` a 4 Hz em vez de um monitor global: zero prompt de TCC novo.
- Notificações de wake/sessão (`NSWorkspace.didWake…`) chegam no `NSWorkspace.shared.notificationCenter`, **não** no `.default` — assinar no center errado = observer que nunca dispara.
- App menubar-only (LSUIElement): usuário pode arrastar o status item pra fora e o macOS PERSISTE a remoção → app roda sem UI alcançável para sempre. `MenuBarExtra(isInserted:)` pinado + heal do flag no launch (T17).

- `Data.removeFirst`/slices deslocam `startIndex` — subscript absoluto (`data[0]`) trapa depois. Parser de stream: sempre indexar relativo a `startIndex` e compactar com `Data(dropFirst)`. (Pegou no `FileChannelDecoder`; testes de frame-inteiro-por-feed não pegam — só o teste byte-a-byte pegou.)
- `NWConnection` presa em `.preparing` (porta morta) nunca vira `.failed` sozinha — timeout de handshake é obrigatório em UDP/DTLS (vale igual p/ TCP/TLS: `FileChannelConnection` reusa o mesmo timeout).
- Enfileirar `send()` público (queue.async) de dentro de um bloco já na queue = mensagem sai depois do teardown. Métodos internos *OnQueue diretos.
- **Confirmado na prática (beta.1→beta.2):** update de app ad-hoc quebra TCC nas duas máquinas — toggle aparece ligado em System Settings mas o preflight falha, e re-conceder NÃO regrava a entrada. Cura: `tccutil reset All dev.crossdesk.mac` + conceder de novo. Prevenção: identidade de assinatura estável.
- Truncar `String.utf8` por contagem de bytes (`.prefix(N)`) pode cortar um escalar multi-byte ao meio — o outro lado falha `String(data:encoding:.utf8)` no fragmento e o parser (que confia em "UTF-8 inválido = payload corrompido") derruba a mensagem INTEIRA, não só o campo truncado. Qualquer campo de string com limite de tamanho no fio precisa recuar até um limite de escalar válido, não só cortar bytes (`WireStrings.utf8Prefix`, 2026-07-05).
- Padrão "listener/browser que se recria sozinho após falha" (retry, bind race, rotação de PSK) precisa de um contador de geração comparado no callback assíncrono — sem isso, um `stop()` chamado entre o agendamento do retry e sua execução deixa o componente reviver depois de "parado". Já resolvido no `DTLSServer` desde o início; replicado em `ServerBrowser` e `TransferCoordinator` nesta revisão (2026-07-05) — vale como checklist para qualquer novo listener nas fases 2/3.
- **VPN corporativa numa das máquinas quebra a sessão P2P local** (achado ao vivo no UAT de 2026-07-06, beta.13): com uma VPN de trabalho ativa num dos Macs, log mostrou o cliente tentando 3 endereços simultâneos (IPv4 LAN + 2 IPv6), `handshake timeout` repetido, `peer timeout` (`TransportTiming.peerTimeout` = 6s, não é agressivo) e `edge crossed → REMOTE`/`LEAVE_REQUEST` flapando em <1s várias vezes — sintoma clássico de VPN forçando/atrapalhando tráfego que deveria ficar 100% na LAN. Não é bug do CrossDesk: **desligar a VPN na máquina que a usa** resolve. Não investigar de novo como "conexão instável" sem primeiro perguntar sobre VPN/rede corporativa nas duas máquinas.

## Lições

- (vazio)

## Preferences

- (herda da raiz)
